# Dev journal — 2026-06-09

## Session: MOO-49 — Real-Time Search community memory (Phase 2 complete-ish)

### What we did

Built and shipped **MOO-49** end-to-end in a clean-context session driven by the focused
handoff (`docs/HANDOFF-MOO-49.md`): brainstorm → spec → plan → subagent-driven TDD →
3-layer review → PR #9 → merge → Fly deploy → live deployed verification → Done.

- **New module `agent/agent/community-memory/`** (4 small files + 28 tests):
  `rts-client.js` (one `assistant.search.context` POST, never throws — HTTP-status
  guarded), `merge.js` (dedup by `channel_id:message_ts`, newest-first, cap 8, plain-text
  rendering), `search.js` (EN+ES parallel fan-out via `Promise.allSettled`, partial-failure
  note, fallback instruction text), `tool.js` (`search_community_memory` SDK tool on an
  in-process `community-memory` MCP server, text-only results).
- **The prod gotcha fixed:** `buildAgentOptions()` extracted from `runAgent` resolves
  `deps.userToken ?? process.env.SLACK_USER_TOKEN` — the deployed bot-token app never
  populates `context.userToken`, which is why slack-mcp showed disconnected in prod. The
  Fly secret now lights up RTS *and* slack-mcp. Bot token still does all posting.
- **COMMUNITY MEMORY prompt section**: agent-decided trigger ("call it even if they don't
  explicitly ask"), native EN+ES query generation, 💬 community memory beside 📋 official
  record, slack-mcp fallback instruction, never-stored restated to the model.

### Decisions (brainstormed, all recommended options accepted)

1. **Primary = in-process SDK tool** (own the EN/ES merge + no-persistence guarantee,
   unit-testable); slack-mcp stays the fallback per the acceptance criteria.
2. **Trigger = agent-decided, prompt-nudged** — zero trigger code.
3. **Rendering = agent-composed text sections** through the existing streamer — no new
   Block Kit layer.
4. `GAVEL_DISABLE_RTS=1` as the deterministic fallback-forcing switch for demos/tests.

### Verification highlights (evidence, not assertions)

- Live RTS through the real composed stack found a real workspace message
  (`[2026-06-08] in <#C0B8KS5VCCC>` + permalink).
- Forced fallback returns the slack-mcp instruction with **zero** fetch calls (tested).
- No-persistence: zero Convex imports / `console.*` in the module; verified by grep plus
  two independent reviewer passes over the whole diff (Slack ToS rule intact).
- 118/118 tests, Biome clean, final whole-branch review verdict READY FOR PR.
- Deployed test: Tarik DM'd the live bot; Gavel answered with official-record reasoning
  and a proactive follow-up offer. MOO-49 → Done with the evidence comment.

### Learnings / gotchas retired

- **App-issued user tokens post with a `bot_id`** (`bot_profile: "Gavel (local)"`), so a
  synthetic "user" DM sent via `SLACK_USER_TOKEN` is skipped by our own bot-message guard.
  E2E thread tests need a real human message; the API can only *read* the result.
- **The 7-day question (from Tarik's live test):** `get_upcoming_events` caps at 7 days +
  Final-only by design, but the deeper truth is Milwaukee's Legistar **Web API** only
  carries events near agenda publication — a full no-filter query through July 14 returned
  9 events, furthest June 15, all Final. The legistar.com calendar pages (InSite) show
  scheduled dates the API doesn't have yet. Wider windows buy nothing; honest tool
  description wording is the right fix (small follow-up candidate).
- Per-call `createSdkMcpServer` construction is cheap (no transport until `query()` wires
  it; SDK schema registry is a WeakMap) — verified during review, no caching needed.

### Risks retired

- RTS-in-prod (the #1 sponsor-tech risk after sandbox access): now wired, deployed, and
  exercised against the real API with the real token.
- Slack-mcp dead-in-prod mystery (App Home red dot): root cause was the token threading,
  fixed as part of this issue rather than worked around.

### Next steps

- Phase 3: history seeding (richer community-memory demo material), watchlists +
  agenda-change detection (the "auto-check June 29 for you" Gavel offered is exactly this).
- Small follow-up candidate: reword `get_upcoming_events` description to explain the
  publish-near-final reality instead of implying a 7-day feed limit.
- Housekeeping reminder from the handoff: rotate the chat-pasted tokens (including
  `SLACK_USER_TOKEN`).

---

## Session 2: tool-description fix + MOO-43 bilingual (same day)

### What we did

- **Shipped the `get_upcoming_events` reword** (bc6c1e9, deployed to `gavel-app`): the
  tool now explains that Milwaukee publishes meetings to its feed only when the agenda is
  finalized, instead of implying a 7-day feed limit. Closes the loop on Tarik's live
  "why only 7 days?" question from the MOO-49 deployed test.
- **Built and shipped MOO-43** (PR #10, merged 801572a; `gavel-poller` redeployed):
  - Audit-first: criteria 1 & 4 were already satisfied by MOO-42/44 (native bilingual
    summarizer with structured output; identifiers-stay-English rule). Built only the gaps.
  - **Per-channel language gate**: `buildAlertCard` takes `language` ('en' default) — ES
    channels get the EN/divider/🇪🇸-ES card, everyone else EN-only. `processPendingAlerts`
    reads each matched channel's subscription language (MOO-45's field) and builds each
    variant at most once per row.
  - **Glossary**: added variance, conditional use, TIF, permit to the EN→ES civic glossary.
  - TDD inline (no subagent fan-out — small diff): 5 new tests RED→GREEN, 123/123, Biome
    clean. New `scripts/bilingual-verify.mjs` proves it against real matters: same-matter
    EN-only vs EN+ES card check, file numbers + `2000 S 13th St` untranslated, natively
    composed Spanish (real Licenses Committee revocation matter).

### Discovery that mattered

**The Convex `subscriptions` table was empty** — local and the deployed poller share the
same deployment (vivid-weasel-903), so alerts had been silently going nowhere since the
demo row was removed (likely during June 8 drain testing). Re-subscribed `C0B8KS5VCCC`
(ZONING/N&D + City Plan Commission + Licenses; keywords rezoning/demolition/zoning;
**language: 'es'**) so the bilingual showcase keeps working post-gate and alerts flow
again this week. Lesson: "no alerts" and "nothing matched" look identical — a
zero-subscriptions warning in the drain would be a cheap guard.

### Learnings

- Audit-before-build pays: half of MOO-43's criteria were already met; the build was a
  ~100-line diff instead of a rebuild.
- Behavior change worth remembering: channels without `language: 'es'` now get EN-only
  cards (previously everything was bilingual) — language must be set per channel.

### Open

- **MOO-43 stays In Review** pending the native-ES-speaker fluency review of ≥3 real
  cards (`node scripts/bilingual-verify.mjs` prints fresh ones; today's texts are on the
  issue). Doubles as the submission's community-engagement line.
- Token rotation still the standing housekeeping item (pinned in `.remember/remember.md`).
- Next unblocked: MOO-46 (slash commands) or Phase 3 via MOO-54 (sandbox seeding).
